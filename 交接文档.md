# 旅行相册 3D 粒子地球 · 交接文档

## 项目概况

一个 3D 粒子地球旅行相册 Web 应用。纯前端，数据存在 IndexedDB。

- 入口：`index.html`（用 http-server 跑，端口 8081）
- 技术栈：Three.js (CDN) + 原生 JS + IndexedDB
- 项目路径：`D:\大学作业文件夹\自制软件\旅行相册`

## 当前文件结构

```
旅行相册/
├── index.html                 # 主页面（黑白灰 UI）
├── css/style.css              # 样式
├── js/
│   ├── app.js                 # 主入口 + UI 逻辑
│   ├── earth.js               # Three.js 粒子地球核心
│   └── data.js                # IndexedDB 数据层
├── data/
│   ├── cities.json            # 中国县市经纬度（~800条）
│   └── map/
│       ├── coastline.geojson  # 全球海岸线（本地）
│       ├── borders.geojson    # 国界线（本地）
│       └── china_provinces.geojson  # 中国省界（本地）
└── 交接文档.md
```

## 已实现功能

### 3D 地球
- 粒子渲染：海岸线（白色, size=1.2）、国界线（白色, size=1.2）、中国省界（白色, size=1.5）
- 所有粒子使用 `PointsMaterial` + `sizeAttenuation: false`（固定像素大小）
- 地球自动绕 Y 轴旋转（speed=0.0004），空格键暂停/恢复
- OrbitControls：鼠标拖拽旋转、滚轮缩放（距离 1.8~6）
- 赤道粒子环（`scene` 层，不随地球旋转，300 粒子，opacity=0.2）
- 星空背景粒子 1000 个（`scene` 层）
- 常住地发光粒子（Sprite + 径向渐变纹理，脉动动画）
- 流星效果：随机从海岸线某点出发，沿地表切向划过，拖 10 段尾巴，约 0.6s 消失，~3s 触发一次

### 粒子动画
- 海岸线加载时从 0 渐显到 0.8（约 2.2s），之后持续呼吸（0.20~0.80，周期~8s）
- 国界线/省界线缩放控制 + 平滑过渡（lerp rate=0.08）
- 国界线：dist<5 时显示 opacity=0.8，否则隐藏
- 中国省界：dist<4.5 时显示 opacity=0.4，否则隐藏

### 地点管理
- 中国城市搜索（从 cities.json 中按名称/省份匹配）
- 添加/编辑/删除地点（名称、日期、评分、感想）
- 从常住地到目的地的贝塞尔弧线
- 点击地球上的地点粒子簇弹出详情卡片
- 评分：1~5 星，粒子簇大小和亮度随评分变化
- 照片：上传（多张）、缩略图预览、放大查看、说明文字编辑

### 数据
- IndexedDB 持久化（meta 表存设置，places 表存地点）
- 导出数据为 JSON 文件下载
- 所有地图数据本地存储，无需网络

### UI
- 黑白灰配色，无彩色
- 白色三角▼下拉菜单（设置常住地、导出、重置视角）
- [+] 按钮添加地点

## 未实现 / 待办

### 功能缺失
1. **地点列表侧边栏**：最初设计有侧边栏列出所有地点，但现在只有点击地球才弹详情卡片。用户没再提这事，不确定还要不要加
2. **部署到 GitHub Pages**：未部署
3. **照片裁剪/旋转/滤镜**：用户明确说"不要"（原设计有 `photo-editor.js`）

### 需要验证的问题（用户刚改完还没确认）
1. 中国省界缩放时是否能正常显示（刚换成本地 `china_provinces.geojson`）
2. 海岸线呼吸效果是否可见
3. 流星效果是否满意
4. 常住地发光粒子是否满意
5. 加载速度是否改善（地图数据后台异步加载）

### 已知问题
1. `china_provinces.geojson` 中包含"南海诸岛"（South China Sea islands），这是个从大陆延伸到远海的 Polygon，投影到 3D 球体上看起来可能是一条长线延伸到海里
2. 照片 base64 存在 IndexedDB 里，照片多了会占用大量空间

## 用户画像

- 称呼：直接骂人型用户，说话带脏字，但要求清晰
- 技术背景：非专业开发者，但知道自己要什么
- 沟通风格：直接、不耐烦、不喜欢废话、不接受模棱两可
- 关键原则：**操作前先说清楚再动手**，不要闷头改代码
- 语言：全中文沟通
- 所在地区：推测在中国大陆（GitHub raw 被墙，jsdelivr 部分可用）
- 不喜欢的：emoji、花哨颜色、绕弯子的解释

## Key 技术决策

1. **sizeAttenuation**：大陆轮廓/边界用 `false`（固定像素），地点粒子簇用 `true`（随缩放变化）
2. **数据源**：全部本地 `data/map/` 目录，三个 GeoJSON 文件
3. **异步加载**：`app.js` 中 `earth.start()` 先启动渲染，`loadCoastlines()` / `loadAdminBoundaries()` 后台异步加载，不阻塞界面
4. **缩放阈值**：国界线 dist<5，省界线 dist<4.5，初始相机距离 ~3.81（所以国界线默认可见，省界线需要拉近一点）
5. **Zoom 代码**：在 `fadingItems` 全部完成后才生效，避免与加载淡入打架

## 历史沿革（踩过的坑）

1. ~~手画多边形大陆轮廓~~ → Natural Earth GeoJSON 数据
2. ~~topojson-client mesh() 解析省界~~ → 直接下载中国省界 GeoJSON
3. ~~GitHub raw 加载地图~~ → 全本地文件
4. ~~Sprite 画地球轮廓~~ → Points 粒子环（和大陆轮廓一致）
5. ~~实心球体常住地~~ → 发光 Sprite + 脉动
6. ~~单点闪烁~~ → 流星划过
7. ~~全局 await 地图数据再启动~~ → 异步加载不阻塞

## 用户之前提过但你可能需要注意的

- 所有粒子默认白色，不要彩色
- 永久禁止 emoji（用户明确反感）
- 粒子不随缩放改变大小（用 `sizeAttenuation: false`）
- 地球轮廓要有，用粒子，不能太突兀
- 空格键切换旋转
- 输入框说"撤销"就是让代码撤回，别想复杂了
