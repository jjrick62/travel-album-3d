# Colorful · Meridian — 开发日志

## 2026-04-30 / 2026-05-01 凌晨

### 完成内容

**背面粒子半透明**
- 方案：双份 Mesh + depthFunc（正面 LessEqualDepth，背面 GreaterDepth）
- 背面粒子以正面 35% 亮度渲染，全缩放级别生效
- 深度遮挡球始终满尺寸写深度

**粒子呼吸优化**
- 海岸线常亮 0.8，不再呼吸
- 国界、省级、市级、县级粒子各自以不同频率呼吸
- 呼吸叠加在缩放透明度之上

**悬浮卡片系统**
- HTML + SVG 覆盖层，每帧 3D→2D 投影定位
- 卡片从地点沿径向往外 150px，细线连接地表到卡片
- 背面卡片缩小 + 渐隐（根据 facing 值平滑过渡）
- 位置平滑 lerp + 0.6px 吸附防抖

**卡片聚类与展开**
- 滞回逻辑：120px 内合群，已合群的 180px 才拆
- 折叠时显示代表卡片 + CSS 伪元素叠放阴影
- 代表选择：区>市>省层级优先 + 缓存在 state 防闪动
- 展开：CSS transition 弹簧曲线径向散开
- 父行政区有子卡片时跳过父卡 + 同名去重

**卡片交互**
- 单击卡片 → 摄像机飞到地点（暂停自转）
- 双击卡片 → 飞到地点 + 弹出编辑框
- 已聚焦再单击 → 直接进入编辑
- 点折叠卡组 → 飞过去再展开
- 点任何非卡片区域 → 保持当前角度拉远回总览

**摄像机飞行**
- 聚焦时暂停自转，地点保持在画面正中
- 编辑完成后从地点沿径向向后拉远，拉到位后恢复自转
- 点空白回退时保持当前视角方向，纯缩小

**编辑体验**
- 编辑卡片延迟弹出（等摄像机到位后回调弹出）
- 编辑时填区粒子呼吸高亮
- 编辑弹窗新增删除按钮

**其他**
- 常住地光点缩小（base 0.04），距离补偿锁定大小
- 地点光点同样距离补偿锁定大小
- 常住地行政区填区粒子（延迟加载 GeoJSON 后补建）
- 流星固定东向滑行
- 搜索框纯黑背景 + 灰色 placeholder
- 项目名改为 Colorful · Meridian
- 部署 GitHub Pages：https://jjrick62.github.io/travel-album-3d/

### 待改进

**代码结构**
- `earth.js`（~800 行）和 `app.js`（~900 行）过于臃肿
- 悬浮卡片系统应抽成独立模块 `cards.js`
- 摄像机飞行逻辑可抽成 `camera.js`
- 聚类/动画状态管理散落在函数属性上（`updatePlaceCards._clusters` 等），不够清晰

**性能**
- 每帧 `querySelector` 遍历所有卡片 DOM，地点多时会有性能瓶颈
- `china_districts.geojson`（23MB）和 `world_admin1.geojson`（17MB）国内加载慢
- 无 Service Worker，无离线支持

**移动端**
- 没有响应式布局适配
- 小屏幕上浮窗卡片拥挤，聚类阈值未根据屏幕尺寸调整
- 触摸交互未区分单指/双指（与 OrbitControls 冲突）

**功能缺失**
- 只有导出没有导入（导入函数在 data.js 里写好了但没暴露 UI）
- 没有搜索/筛选地点列表
- 没有数据统计面板（最远距离、最多照片等）

### 部署

- GitHub Pages: https://jjrick62.github.io/travel-album-3d/
- 数据存储在浏览器 IndexedDB，与 GitHub Pages 无关
- 换设备需导出/导入 JSON
