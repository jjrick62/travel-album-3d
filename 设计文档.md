# 旅行相册 · 设计文档

## 概述

一个 3D 地球粒子模型的旅行相册网页应用。地球缓缓转动，用户从预置的中国县市列表中选择去过的地方，粒子填充对应地区，拉线连接到地点，预览照片、打分、写感想。纯前端，可部署到 GitHub Pages 分享给朋友。

## 技术选型

| 层面 | 技术 | 原因 |
|------|------|------|
| 3D 渲染 | **Three.js**（CDN 引入） | 粒子地球、旋转、连线，Web 最成熟的 3D 库 |
| 数据存储 | **IndexedDB**（idb 库） | 照片 base64 量大，localStorage 不够用 |
| UI | 纯 **HTML + CSS** | 不用框架，减少依赖，直接写 |
| 打包 | 无，直接 HTML 文件 | 方便部署和分享 |

> 纯前端应用，不涉及后端，所有数据存在浏览器里。部署到 GitHub Pages 就能给朋友看。

## 功能模块

### 1. 3D 粒子地球（核心视觉）
- 纯黑色背景，地球在空间中飘浮
- **粒子勾勒出大陆轮廓和省级/地区界线**，不画经纬度网格线
- 背景少量星空粒子点缀
- 地球自动绕 Y 轴缓慢旋转
- 鼠标拖拽可手动旋转视角，滚轮缩放
- **去过的地区用粒子填充**，颜色按用户设置的主题色
- 同一颜色按评分不同用**不同深浅**区分

### 2. 常住地标记
- 用户从中国县市列表中选择自己的"家"
- 地球上用**最大的白色单颗球体**标记
- 支持后续修改

### 3. 旅行地点管理
- **从预置的中国县市列表中选择**，精确到县级
- 搜索框快速筛选（拼音/汉字都支持）
- **去过的地区用不同颜色粒子填充**，颜色按用户自定义主题
- **同一颜色按评分深浅区分**（评分越高颜色越饱和/亮）
- 从"家"到每个地点拉一条细线（弧线，贴合地球曲率）
- 点击地球上的填充区域或连线 → 弹出详情
- 现阶段只做国内，不涉及国外

### 4. 地点详情卡片（浮层）
- 默认**隐藏**，只有地球缓缓转动
- **点击地点填充区域或弧线** → 从侧边滑出详情卡片
- 地点名称、星级评分（1-5 星，点击评分）
- 照片列表（缩略图网格）
- 感想/笔记（纯文本）
- 访问日期
- 编辑和删除功能
- 点击空白区域或关闭按钮 → 卡片收回

### 5. 照片管理（精简版）
- 上传照片（点击上传）
- 照片预览（点击缩略图放大）
- 照片说明文字
- ~~无滤镜、无裁剪、无编辑~~（不需要）

### 6. 评分系统
- 每个地点 1-5 星评分
- 在地球上填充粒子颜色按评分**不同深浅**
- 光点大小随评分变化
- 首页统计：去过几个地方、平均评分、最高评分地点

## 中国县市数据

内置一份中国行政区划数据集，包含省/市/县三级，精度到县级：

```
{ "name": "北京市", "level": "province", "lat": 39.9042, "lng": 116.4074, "path": "北京市" }
{ "name": "海淀区", "level": "district", "lat": 39.9561, "lng": 116.3106, "path": "北京市·海淀区" }
{ "name": "杭州市", "level": "city", "lat": 30.2741, "lng": 120.1551, "path": "浙江省·杭州市" }
```

收录约 **800+ 条**（所有地级市 + 主要区县），够日常用。独立 JSON 存放。

## 数据模型

```javascript
{
  theme: { color: "#ff6b6b" },           // 用户自定义主题色
  home: { name: "北京市", lat: 39.9042, lng: 116.4074 },
  places: [
    {
      id: "uuid-xxx",
      name: "杭州市",
      fullName: "浙江省·杭州市",
      lat: 30.2741,
      lng: 120.1551,
      rating: 5,             // 1-5，决定颜色深浅
      notes: "西湖太美了！",
      visitDate: "2025-08-15",
      photos: [
        {
          id: "uuid-yyy",
          dataUrl: "data:image/jpeg;base64,...",
          caption: "西湖日落"
        }
      ]
    }
  ]
}
```

## UI 布局

```
┌─────────────────────────────────────────────────────┐
│  ✈ 我的旅行    统计: 5地 | ★4.2   ⚙️   [+ 添加]    │
├─────────────────────────────────────────────────────┤
│                                                     │
│                                                     │
│                3D 粒子地球（全屏区域）               │
│                                                     │
│    · 大陆轮廓粒子        · 去过的地区彩色填充        │
│    · 自动旋转            · 家→地点弧线              │
│    · 拖拽/缩放           · 点击弹出详情卡片         │
│                                                     │
│                                                     │
│                    ┌──────────────┐                 │
│                    │ 地点详情卡片  │                 │
│                    │ (点击后浮现)  │                 │
│                    │              │                 │
│                    │ ★★★★★       │                 │
│                    │ 📷 照片      │                 │
│                    │ 笔记内容…    │                 │
│                    └──────────────┘                 │
│                                                     │
├─────────────────────────────────────────────────────┤
│  📤 导出数据           🌐 部署到 GitHub Pages        │
└─────────────────────────────────────────────────────┘
```

关键：**无侧边栏**，地球占主导。详情卡片默认隐藏，点击后浮现。

## 文件结构

```
旅行相册/
├── index.html                 # 主页面
├── css/
│   └── style.css              # 全部样式
├── js/
│   ├── app.js                 # 主入口，UI 逻辑
│   ├── earth.js               # Three.js 粒子地球 + 交互
│   └── data.js                # IndexedDB 数据操作
├── data/
│   └── cities.json            # 中国县市经纬度数据集
└── README.md                  # 说明文档
```

> 去掉了 photo-editor.js，因为不需要照片编辑功能。

## 实施计划

### 第一期：3D 地球 + 地点管理（核心骨架）
1. 搭建项目文件结构，引入 Three.js
2. 实现粒子地球（大陆轮廓粒子 + 纯黑背景）
3. 地球旋转 + 鼠标交互（OrbitControls）
4. 嵌入中国县市数据集（cities.json）
5. 实现 IndexedDB 数据层
6. 常住地设置（从列表搜索选择，房子图标）
7. 添加旅行地点（搜索选择城市）
8. 去过的地区粒子填充 + 主题色
9. 评分决定颜色深浅
10. 从家到地点拉弧线
11. 点击地球填充区/弧线 → 浮现详情卡片
12. 地点详情卡片（评分、照片、笔记）
13. 数据导出（JSON 下载）

### 第二期：照片管理 + 打磨
1. 照片上传（File API + IndexedDB 存储）
2. 照片预览（缩略图 + 点击放大）
3. 照片说明文字
4. 背景星空粒子
5. 首页统计信息
6. 部署到 GitHub Pages

## 验证方式

1. 打开 index.html，黑色背景 + 粒子地球轮廓自动旋转
2. 设置常住地 → 地球出现房子图标
3. 添加杭州 → 杭州地区粒子变色填充 + 弧线
4. 再添加几个城市，不同评分 → 颜色深浅不同
5. 点击地球上的彩色区域 → 详情卡片浮现
6. 点击空白处 → 卡片收回
7. 上传照片 → 预览正常
8. F5 刷新 → 数据不丢失
9. 导出数据 → 下载 JSON 文件
