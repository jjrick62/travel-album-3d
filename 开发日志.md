# 开发日志

## 2026-04-29

### 数据层

- **城市搜索数据**：从 DataV GeoAtlas API 拉取全国 363 个地级市 + 2840 个区县的 GeoJSON 边界和中心坐标，补全 `cities.json`（186 → 3229 条），覆盖全国所有省市县
- 新增 `data/map/china_cities.geojson`（3.2MB，363 个市级行政边界）
- 新增 `data/map/china_districts.geojson`（23MB，2840 个县级行政边界，懒加载）
- 搜索改为相关度排序：精准匹配 > 开头匹配 > 包含匹配 > 省份匹配，市级优先

### 3D 地球

- **四级行政边界缩放显隐**：国界 (dist<5.0) → 省界 (dist<3.2) → 市界 (dist<2.1) → 县界 (dist<1.85)，进入阈值即时显示，退出平滑淡出
- 县级数据懒加载：首次缩放到 dist<2.4 时后台下载 23MB 数据，不阻塞渲染
- 边界粒子粗细逐级递减：省 1.5px / 市 0.85px / 县 0.75px，各级抽稀步长递增
- **地点粒子重写**：从 3D 球体 blob 改为多边形内部高密度填充，匹配对应行政区轮廓；无匹配时退化为贴地光斑
- 地点两级显示：远看发光小点（Sprite），放大到对应行政级别点亮轮廓填充

### 交互优化

- **自转 + 拖拽速度随缩放自适应**：`dist/5.0` 比例缩放，放大越近越慢
- 点击空白区域关闭详情卡片
- 保存城市名校验改为智能匹配（模糊搜索自动补全）

### Bug 修复

- 搜索下拉菜单被 `.modal-content` 的 `overflow-y: auto` 裁剪 → 下拉菜单挂到 `<body>`，`position: fixed` + JS 动态定位
- 未设常住地时已添加地点不渲染 → `addPlace` 不再依赖 `homeLocation`，仅弧线依赖
- 新建地点未选评分时粒子不可见 → 默认 3 星
- CSS 星评 hover 方向反向 → 删除冲突 CSS 规则，JS 已有正确处理
- 编辑地点时搜框改名不点选导致坐标错 → 加入输入框与选中城市名校验
- `addPlace` 变量重命名遗漏 → `pos` → `center`

### 部署

- 推送到 GitHub 公开仓库 `https://github.com/jjrick62/travel-album-3d`
